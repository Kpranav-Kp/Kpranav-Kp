name: Update Profile Analytics SVG

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate SVG
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: python scripts/generate_dna.py

      - name: Commit and push
        run: |
          git config user.name "Kpranav-Kp"
          git config user.email "170631339+Kpranav-Kp@users.noreply.github.com"
          git add assets/developer-dna.svg assets/engineering-data.json
          git diff --cached --quiet || git commit -m "chore: update profile analytics SVG"
          git push origin ${{ github.ref_name }}
